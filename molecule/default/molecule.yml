# terraform managed
---
dependency:
  name: galaxy
  options:
    requirements-file: requirements.yaml
driver:
  name: docker
platforms:
  - name: "${IMAGE_NAME}"
    image: "${REGISTRY}/${IMAGE_ID}:latest"
    tmpfs:
      - /run
      - /tmp
    privileged: true
provisioner:
  name: ansible
  inventory:
    host_vars:
      Ubuntu:
        ansible_user: ansible
verifier:
  name: testinfra
